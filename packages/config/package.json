{
  "name": "@carbon/config",
  "version": "1.0.0",
  "description": "Shared configuration for Carbon Emission Calculator",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rimraf dist",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.1.3",
    "rimraf": "^5.0.1"
  },
  "files": [
    "dist",
    "eslint",
    "prettier"
  ]
}